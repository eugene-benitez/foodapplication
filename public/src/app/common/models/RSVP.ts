import { Person } from '../';
export class RSVP {
    _id: string;
    plus: number;
    person: Person[];
    
}

//thought of adding them like the pet skills .. but what if it had more details later on