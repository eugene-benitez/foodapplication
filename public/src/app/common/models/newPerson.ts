export class newPerson {
    fullName: string;
    email: string;
    tel: number;
}

//we can delete this .. if the RSVP person is only a registered user .. could be a good marketing tactic .. but it could back fire .. as people stop using the system entirely 